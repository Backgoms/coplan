Auth settings for coplan.

This opens a small auth status view.

- Shows `codex: login ok` or `codex: login off`.
- If `ok`, selecting it shows a logout hint; pressing Enter logs out.
- Exit with Esc, or select `exit` and press Enter.

Run shell command:

```bash
{{COPLAN_CLI_PATH}} auth
```
