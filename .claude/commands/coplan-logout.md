Log out of Codex/ChatGPT for coplan.

This clears coplan local auth state and also logs out of Codex.

Run shell command:

```bash
{{COPLAN_CLI_PATH}} logout --codex
```

Then run:

```bash
{{COPLAN_CLI_PATH}} status
```
