Update this coplan git clone.

This checks whether the repo is behind origin/main and fast-forwards if possible.

Run shell command:

```bash
{{COPLAN_CLI_PATH}} update --apply
```

Then run:

```bash
{{COPLAN_CLI_PATH}} status
```
